name: Simple HTML CI

# Run on push or pull request to main branch
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  validate-html:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout your repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Validate HTML (dummy command for now)
      - name: Validate HTML
        run: echo "HTML validation would run here!"